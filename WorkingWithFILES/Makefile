.PHONY: clean All

All:
	@echo "----------Building project:[ StringStream - Debug ]----------"
	@cd "StringStream" && "$(MAKE)" -f  "StringStream.mk"
clean:
	@echo "----------Cleaning project:[ StringStream - Debug ]----------"
	@cd "StringStream" && "$(MAKE)" -f  "StringStream.mk" clean
